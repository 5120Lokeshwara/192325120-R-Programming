# Define two vectors
vec1 <- c(1:9)
vec2 <- c(10:18)

# Create an array with 3 rows, 3 columns, and 2 tables
arr <- array(c(vec1, vec2), dim = c(3, 3, 2))

# Print the array
cat("3x3x2 Array:\n")
print(arr)
